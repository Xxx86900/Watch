# STM32-WATCH（HAL）

这是一个基于 STM32F103C8T6、HAL 库和 CMake 的手表固件。项目没有照搬原开源工程的标准库应用代码，而是按“板级支持—器件驱动—服务—应用—UI”重新组织，便于两人继续分工开发。

## 已完成的功能

- SH1106 128×64 OLED 显示与亮度调节
- 三按键消抖、短按、长按与事件队列
- RTC 时间/日期显示和设置，备份域有效时不会因复位丢失
- 开机后立即锁定电源保持脚，不必长时间按住电源键等待 LSE 起振
- 电池 ADC 采样、电压换算和电量估算
- MPU6050 初始化、姿态角、计步、距离和热量估算
- 首页、菜单、运动、水平仪、秒表、手电筒、设置共 7 个页面
- 30 秒无操作熄屏，按任意键唤醒
- 长按首页确认键关闭电源保持
- CMake 自动生成 ELF、HEX 和 BIN 烧录文件

## 工程结构

```text
Watch_project/
├── Core/                  CubeMX 生成的 HAL 初始化代码
├── Drivers/               STM32 HAL/CMSIS
├── Watch/
│   ├── Bsp/               PCB 引脚与基础板级操作
│   ├── Components/        SH1106、MPU6050 器件驱动
│   ├── Services/          按键、RTC、电池、运动服务
│   ├── App/               应用状态机和任务调度
│   ├── UI/                画布、页面和 UI 管理器
│   └── Assets/            字体与图形资源
├── CMakeLists.txt
└── Watch_project.ioc
```

## 硬件连接

| 功能 | MCU 引脚 | 有效电平/说明 |
| --- | --- | --- |
| OLED I2C1 SCL/SDA | PB8/PB9 | I2C1 重映射，地址 0x3C |
| MPU6050 I2C2 SCL/SDA | PB10/PB11 | 地址 0x68 |
| 电池电压 ADC | PA0 | ADC1_IN0 |
| 确认/电源键 | PA4 | 低电平按下 |
| 下一项键 | PA6 | 低电平按下 |
| 上一项键 | PB1 | 低电平按下 |
| 电池检测使能 | PB12 | 低电平使能 |
| 电源保持 | PB13 | 高电平保持供电 |
| 手电筒 LED | PB15 | 低电平点亮 |
| RTC 晶振 | PC14/PC15 | 32.768 kHz LSE |

I2C1 和 I2C2 当前配置为 400 kHz。若实物出现偶发通信错误，先检查焊接、地址和上拉电阻；必要时可在 CubeMX 中降为 100 kHz 后重新生成。

## 按键操作

- 首页：短按确认进入菜单；长按确认关闭电源保持。
- 菜单：上一项/下一项选择，确认进入；长按确认返回首页。
- 运动：上一项返回，下一项清零计步数据。
- 水平仪：上一项返回，确认校准当前水平面。
- 秒表：确认开始/暂停；暂停时下一项清零；上一项返回。
- 手电筒：确认或下一项开关 LED；上一项关闭并返回。
- 设置：上一项/下一项选择；确认修改亮度或编辑时间日期；编辑时上一项/下一项调整，确认进入下一字段并保存；长按确认取消编辑或返回。
- 熄屏后：任意按键只负责唤醒，需再次按键执行功能，防止误操作。

## 编译

前提：`arm-none-eabi-gcc`、CMake 和 Ninja 已加入环境变量。

```powershell
cmake --preset Debug
cmake --build --preset Debug
```

生成目录为 `build/Debug/`：

- `Watch_project.elf`：调试文件
- `Watch_project.hex`：推荐使用的烧录文件
- `Watch_project.bin`：裸二进制，烧录起始地址为 `0x08000000`

发布版体积更小：

```powershell
cmake --preset Release
cmake --build --preset Release
```

## 烧录

使用 ST-LINK 连接 SWDIO、SWCLK、GND 和 3.3V 参考电压。在 STM32CubeProgrammer 中选择 ST-LINK，连接后烧录 `build/Debug/Watch_project.hex`，然后复位运行。

## 首次上电检查顺序

1. 不装电池，先用万用表确认 3.3V 电源没有短路。
2. 用限流电源供电，确认待机电流没有明显异常。
3. 连接 ST-LINK，确认能识别 STM32F103C8T6 并烧录。
4. 检查 OLED 是否点亮；不亮时优先检查 I2C1 地址、PB8/PB9 焊接和上拉。
5. 检查三个按键方向是否正确，确认首页长按关机有效。
6. 检查 RTC 走时；完全断开主电源后确认备份电池能保持时间。
7. 摇动手表检查姿态和计步；方向相反时再调整 MPU6050 安装方向映射。
8. 最后接入锂电池，使用万用表对照 UI 电量判断采样是否合理。

## CubeMX 重新生成注意事项

- 以 `Watch_project.ioc` 为准重新生成，保持工程类型为 CMake、HAL 库。
- `Core/Src/main.c` 和 `Core/Src/rtc.c` 中的应用接入代码位于 `USER CODE` 区域，会被保留。
- `Core/Src/i2c.c` 为生成文件，不应单独手改；I2C 速率请在 `.ioc` 中修改后生成。
- CubeMX 不管理 `Watch/` 目录和根目录 `CMakeLists.txt` 中的自定义源文件列表，重新生成后仍需确认 CMake 配置未被覆盖。

## 当前限制

- 电量百分比是按常见单节锂电池放电曲线估算，不等同于专业电量计；成品需要结合实测电压校准。
- 计步算法适合课程项目和功能验证，不是医疗或运动级精度。
- 实物引脚方向、OLED 型号或 MPU6050 安装方向若与原理图/PCB 不一致，需要按实物调整驱动参数。
